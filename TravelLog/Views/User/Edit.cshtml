@model IEnumerable<TravelLog.ViewModels.UserAccountViewModel>

@{
    ViewData["Title"] = "會員列表";
}

<h2>會員列表 (顯示+編輯)</h2>

<table border="1" cellpadding="5" cellspacing="0">
    <thead>
        <tr>
            <th>帳號名稱</th>
            <th>信箱</th>
            <th>手機</th>
            <th>是否啟用</th>
            <th>創建日期</th>
            <th>操作</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr data-userid="@item.UserId">
                <td>
                    <input type="text" name="UserName" value="@item.UserName" />
                </td>
                <td>
                    <input type="text" name="UserEmail" value="@item.UserEmail" />
                </td>
                <td>
                    <input type="text" name="UserPhone" value="@item.UserPhone" />
                </td>
                <td>
                    <input type="checkbox" name="UserEnabled" @(item.UserEnabled ? "checked" : "") />
                </td>
                <td>@item.UserCreateDate.ToString("yyyy/MM/dd")</td>
                <td>
                    <!-- 隱藏相關 ID 資料 -->
                    <input type="hidden" name="UserId" value="@item.UserId" />
                    <input type="hidden" name="UserPdId" value="@item.UserPdId" />
                    <input type="hidden" name="UserPdPasswordHash" value="@item.UserPdPasswordHash" />
                    <input type="hidden" name="UserPdToken" value="@item.UserPdToken" />

                    <button class="btn-save">儲存</button>
                </td>
            </tr>
        }
    </tbody>
</table>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        // 當按下儲存按鈕時，利用 AJAX 送出修改資料
        $(document).ready(function () {
            $('.btn-save').click(function (e) {
                e.preventDefault();
                var $row = $(this).closest('tr');
                // 從同一列讀取值
                var data = {
                    UserId: $row.find('input[name="UserId"]').val(),
                    UserName: $row.find('input[name="UserName"]').val(),
                    UserEmail: $row.find('input[name="UserEmail"]').val(),
                    UserPhone: $row.find('input[name="UserPhone"]').val(),
                    UserEnabled: $row.find('input[name="UserEnabled"]').is(':checked'),
                    UserPdId: $row.find('input[name="UserPdId"]').val(),
                    UserPdPasswordHash: $row.find('input[name="UserPdPasswordHash"]').val(),
                    UserPdToken: $row.find('input[name="UserPdToken"]').val()
                };

                $.ajax({
                    url: '@Url.Action("Edit", "User")',
                    type: 'POST',
                    contentType: 'application/json',
                    data: JSON.stringify(data),
                    success: function (response) {
                        if (response.success) {
                            alert("更新成功");
                        } else {
                            alert("更新失敗");
                        }
                    },
                    error: function () {
                        alert("更新時發生錯誤");
                    }
                });
            });
        });
    </script>
}
